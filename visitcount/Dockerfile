FROM openjdk:11
VOLUME /tmp
ARG CONNECTION
ENV INSTANCE_CONNECTION=$CONNECTION
ARG DATABASE
ENV DATABASE_NAME=$DATABASE
ARG USER
ENV DB_USER=$USER
ARG PASS
ENV DB_PASSWORD=$PASS
RUN echo "$DATABASE_NAME"
ADD target/*.jar app.jar
ENTRYPOINT ["java","-jar","/app.jar"]
